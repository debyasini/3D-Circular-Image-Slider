# 3D-Circular-Image-Slider
A 3D image carousel built using HTML and CSS where images are arranged in a circular layout and rotate smoothly in 3D space. The project uses CSS transforms (rotateY, translateZ) and perspective to create a dynamic rotating gallery without JavaScript. It demonstrates the use of CSS 3D transforms and animations to build interactive UI components.
